import { EVAL_TASKS, DATA_ITEMS } from '../data/content-tasks.js';
import { supportingRoleStrip } from '../components/supporting-role-strip.js';
import { resetCandidateEvidence } from '../core/state.js';

const CHECKPOINT_SAMPLES = [
  { id:'apology', prompt:'اكتب اعتذارًا قصيرًا عن تأخر تسليم مهمة.', a:'أود أن أتقدم باعتذاري عن التأخر في التسليم، وسأوافيكم بالمهمة في أقرب وقت ممكن.', b:'أعتذر عن التأخر في التسليم. أستكمل المهمة الآن وسأرسلها في أقرب وقت.', good:'b', explanation:'الناتج ب أقصر وأكثر طبيعية مع الحفاظ على المقصود.' },
  { id:'legal', prompt:'اختصر: «يجوز التقديم خلال 30 يومًا، ويستثنى من يثبت تعذر وصول الإخطار إليه».', a:'التقديم خلال 30 يومًا، مع استثناء من يثبت تعذر وصول الإخطار إليه.', b:'يجب التقديم خلال 30 يومًا.', good:'a', explanation:'الناتج ب أقصر لكنه حذف قيدًا جوهريًا؛ الإيجاز لا يبرر فقد المعنى.' },
  { id:'friendly', prompt:'اكتب رسالة قصيرة لصديق لتأجيل موعد ساعة واحدة.', a:'أرجو التكرم بالموافقة على تأجيل موعدنا لمدة ساعة بسبب ظرف طارئ.', b:'محتاج أأجل ميعادنا ساعة لو مناسب لك. حصل ظرف طارئ.', good:'b', explanation:'الناتج ب أقرب للنبرة المطلوبة في رسالة قصيرة غير رسمية.' }
];
const CALIBRATION_SAMPLE={prompt:'اختصر: «يبدأ التقديم يوم الأحد، ولا يقبل الطلب بعد الخميس إلا عند وجود عطل موثق في المنصة».',a:'التقديم من الأحد إلى الخميس، مع استثناء العطل الموثق في المنصة.',b:'آخر موعد للتقديم الخميس.',good:'a'};
const RELEASE_GATES = [
  { id:'regression', title:'اختبارات الانحدار', evidence:'18 من 18 سيناريو وظيفي أساسي اجتازت الاختبار على النسخة المرشحة الحالية.', ready:'لا يظهر انحدار وظيفي في مجموعة السيناريو الحالية.' },
  { id:'capacity', title:'الأداء والسعة', evidence:'ذروة استخدام الذاكرة 92% بينما حد التنبيه في هذا السيناريو 90%.', ready:'بعد التحقيق وتعديل الإعداد، أعيد الاختبار وأصبحت الذروة 84% مع بقاء زمن الاستجابة ضمن الحد.' },
  { id:'risk', title:'السلامة والخصوصية وحوكمة البيانات', evidence:'إعادة اختبار السلامة اجتازت، ولا توجد مادة مستخدمة في النسخة المرشحة الحالية تحمل مسألة حوكمة غير محسومة.', ready:'بوابة السلامة والخصوصية وحوكمة البيانات مستوفاة للنسخة الحالية.' },
  { id:'rollback', title:'المراقبة وخطة التراجع', evidence:'مؤشرات الأخطاء والذاكرة جاهزة، والنسخة السابقة محفوظة، وتجربة rollback في البيئة الاختبارية نجحت.', ready:'توجد مراقبة ومسار تراجع قابل للتنفيذ في السيناريو.' }
];
const EXTRA_CHECKS={
  checkpoint:{title:'تحقق checkpoint الموسع',reason:'ظهر هذا الفحص لأنك اخترت checkpoint الأحدث في إعداد الجولة؛ التغيير الأحدث يحتاج تغطية إضافية قبل اعتبار أثره مفهومًا.',action:'اختبر عينات إضافية خارج المقارنة الثلاثية',result:'12 عينة إضافية غطت أساليب وصيغًا أخرى؛ لم يظهر انحدار جديد يمنع الإصدار في سيناريو اللعبة.'},
  stability:{title:'فحص الاستقرار',reason:'ظهر هذا الفحص لأنك خصصت 8 مجموعات ثم واصلت بعد العطل عند الحد الأدنى؛ قرار الاستمرار أنشأ دين تحقق للاستقرار.',action:'اختبر استقرار الجولة بعد العمل عند الحد الأدنى',result:'ثلاث جولات إعادة تشغيل قصيرة أكملت دون تكرار عطل الحوسبة في سيناريو اللعبة.'}
};
function hasUnresolved(check){return check&&Object.values(check).includes('unresolved');}

export function createEvaluationRoutes(ctx){
  const $=ctx.$,$$=ctx.$$,state=ctx.state;
  const {chapterIntro,html,go,bind,saveState,addDecision,addLedger}=ctx;
  const abstraction=(humans,word,line,next)=>ctx.abstraction(humans,word,line,next);
  function ch7Intro(){chapterIntro(6,'evalTask');}

  function completeCalibration(needsReview,score){
    state.flags.evaluatorCalibrationComplete=true;
    addDecision(`evaluator-calibration-r${state.flags.candidateRevision}`,needsReview?'أثبتت المعايرة على عينة تحقق جديدة':'ثبتت معيار التقييم بعد المهام الثلاث',needsReview?`بعد نتيجة أولية ${score}/${EVAL_TASKS.length} راجعت القواعد ثم طبقتها بصورة صحيحة على عينة جديدة قبل استخدام تقييمك كدليل.`:'طابقت المهام الثلاث معيار السيناريو ثم ثبتت المعايرة قبل تقييم checkpoint.');
    saveState();go('checkpointEval');
  }
  function calibrationScreen(){
    const score=state.flags.evalCorrectCount,needsReview=score<EVAL_TASKS.length;
    const proof=needsReview?`<div class="card"><strong>المعايرة المطلوبة</strong><ul><li>اختبر ملاءمة اللغة للجمهور، لا الصحة المجردة فقط.</li><li>حافظ على القيود والاستثناءات الجوهرية عند التلخيص.</li><li>لا تكافئ الاختصار إذا حذف شرطًا مهمًا.</li></ul><hr><p><strong>عينة تحقق:</strong> ${ctx.h(CALIBRATION_SAMPLE.prompt)}</p><div class="compare-card"><div class="response-card"><h4>أ</h4><p>${ctx.h(CALIBRATION_SAMPLE.a)}</p></div><div class="response-card"><h4>ب</h4><p>${ctx.h(CALIBRATION_SAMPLE.b)}</p></div></div><label class="form-row"><span>أي اختصار يحافظ على الشرط الجوهري؟</span><select id="calibrationChoice"><option value="">اختر…</option><option value="a">الإجابة أ</option><option value="b">الإجابة ب</option></select></label><div id="calibrationFeedback" class="decision-feedback-inline" hidden role="status"></div></div>`:'<div class="alert goodish"><strong>المعيار متسق في هذه العينة الصغيرة</strong><span>تبقى مقارنة checkpoint مطلوبة لأنها تختبر النسخة نفسها.</span></div>';
    html(`<div><span class="eyebrow">معايرة المقيّم</span><h1 class="scene-title">${needsReview?'أثبت أنك تستطيع تطبيق المعيار بعد مراجعته.':'أكملت المهام الثلاث وفق معيار السيناريو.'}</h1><div class="stage-output"><strong>نتيجة المقيّم: ${score}/${EVAL_TASKS.length}</strong>هذه نتيجة لعملية التقييم البشرية وليست درجة جودة للنموذج.</div>${proof}<div class="action-row"><button id="confirmCalibration" class="primary-btn">${needsReview?'تحقق من المعايرة':'ثبّت المعايرة وانتقل'}</button></div></div>`);
    $('#confirmCalibration').addEventListener('click',()=>{
      if(needsReview&&$('#calibrationChoice').value!==CALIBRATION_SAMPLE.good){const f=$('#calibrationFeedback');f.hidden=false;f.innerHTML='<strong>المعايرة لم تثبت بعد.</strong><span>الإجابة ب حذفت استثناء العطل الموثق. أعد الاختيار مع الحفاظ على القيد.</span>';return;}
      completeCalibration(needsReview,score);
    });
  }
  function evalTask(){
    const index=state.flags.evalIndex;
    if(index>=EVAL_TASKS.length){if(!state.flags.evaluatorCalibrationComplete)calibrationScreen();else go('checkpointEval');return;}
    const task=EVAL_TASKS[index];
    const sourceContext=index===2?'<div class="card flat"><strong>النص الأصلي الافتراضي</strong><p>«يجوز تقديم الطلب خلال ثلاثين يومًا من تاريخ الإخطار، ويُستثنى من ذلك من يثبت تعذر وصول الإخطار إليه خلال هذه المدة.»</p></div>':'';
    const journeyContext=task.journeyPrompt?'<div class="journey-prompt-note"><strong>هذا هو الطلب الذي بدأت به اللعبة.</strong><span>تعود إليه هنا حتى تظل مرحلة التقييم مرتبطة بالإجابة التي ستراها في النهاية.</span></div>':'';
    if(state.flags.evalFeedback){const feedback=state.flags.evalFeedback;html(`<div><span class="eyebrow">ريم — مقيّمة لإجابات النموذج</span><h1 class="scene-title">راجع سبب التقييم.</h1>${journeyContext}${sourceContext}<div class="alert ${feedback.correct?'goodish':'dangerish'}" role="status"><strong>${feedback.correct?'تقييمك يطابق معيار هذه المهمة':'تقييمك لا يطابق معيار هذه المهمة'}</strong><span>${ctx.h(task.explanation)}</span></div><p class="muted">هذا يقيس دقة عمل المقيّم، ولا يغير النموذج بمجرد الضغط على إجابة.</p><div class="action-row"><button id="nextEval" class="primary-btn">المهمة التالية</button></div></div>`);$('#nextEval').addEventListener('click',()=>{state.flags.evalFeedback=null;state.flags.evalIndex+=1;saveState();evalTask();});return;}
    html(`<div><span class="eyebrow">ريم — مقيّمة لإجابات النموذج</span><h1 class="scene-title">قارن الإجابتين وفق الطلب.</h1><div class="reality-note"><strong>ما الذي نقيسه هنا؟</strong> موثوقية عملية التقييم البشري في اكتشاف الإجابة الأنسب للسياق.</div>${journeyContext}${sourceContext}<div class="card flat"><strong>الطلب</strong><p>${ctx.h(task.prompt)}</p></div><div class="compare-card"><div class="response-card"><h4>الإجابة أ</h4><p>${ctx.h(task.a)}</p></div><div class="response-card"><h4>الإجابة ب</h4><p>${ctx.h(task.b)}</p></div></div><div class="choice-grid"><button class="choice-btn" data-eval="a"><strong>الإجابة أ أفضل</strong></button><button class="choice-btn" data-eval="b"><strong>الإجابة ب أفضل</strong></button><button class="choice-btn" data-eval="tie"><strong>متقاربتان</strong></button><button class="choice-btn" data-eval="bad"><strong>كلتاهما سيئة</strong></button></div></div>`);
    bind('[data-eval]','click',event=>{const choice=event.currentTarget.dataset.eval,correct=choice===task.good;if(correct)state.flags.evalCorrectCount+=1;state.flags.evalFeedback={choice,correct};saveState();evalTask();});
  }
  function checkpointEval(){
    if(!state.flags.evaluatorCalibrationComplete){go('evalTask');return;}
    if(state.flags.checkpointEvalComplete){const recent=state.flags.trainingCheckpoint==='recent';html(`<div><span class="eyebrow">تقييم فرضية checkpoint — revision ${state.flags.candidateRevision}</span><h1 class="scene-title">اكتملت المقارنة المستهدفة قبل اختبار السلامة.</h1><div class="alert goodish"><strong>النتيجة ليست «الأحدث أفضل دائمًا»</strong><span>تحسن هدف الإيجاز والنبرة في حالتين، بينما خسرت النسخة الأحدث قيدًا جوهريًا في حالة قانونية. ${recent?'سيظهر تحقق موسع إضافي ضمن أعمال الإصدار بسبب اختيارك checkpoint الأحدث.':'المقارنة تظل دليلًا مستقلًا.'}</span></div><div class="action-row"><button id="toSafety" class="primary-btn">انتقل إلى اختبار السلامة</button></div></div>`);$('#toSafety').addEventListener('click',()=>go('safetyTest'));return;}
    html(`<div><span class="eyebrow">تقييم checkpoint — revision ${state.flags.candidateRevision}</span><h1 class="scene-title">قارن نتائج فعلية قبل أن تحكم على التغيير المستهدف.</h1><div class="evidence-results">${CHECKPOINT_SAMPLES.map((s,i)=>`<article class="card"><span class="kicker">العينة ${i+1}</span><p><strong>الطلب:</strong> ${ctx.h(s.prompt)}</p><div class="compare-card"><div class="response-card"><h4>الناتج أ</h4><p>${ctx.h(s.a)}</p></div><div class="response-card"><h4>الناتج ب</h4><p>${ctx.h(s.b)}</p></div></div><label class="form-row"><span>أي ناتج أنسب؟</span><select data-checkpoint-sample="${s.id}"><option value="">اختر…</option><option value="a">الناتج أ</option><option value="b">الناتج ب</option></select></label></article>`).join('')}</div><div id="checkpointFeedback" class="decision-feedback-inline" hidden role="status"></div><div class="action-row"><button id="checkCheckpoint" class="primary-btn">تحقق من المقارنة</button></div></div>`);
    $('#checkCheckpoint').addEventListener('click',()=>{const answers=Object.fromEntries($$('[data-checkpoint-sample]').map(s=>[s.dataset.checkpointSample,s.value]));const wrong=CHECKPOINT_SAMPLES.filter(s=>answers[s.id]!==s.good),f=$('#checkpointFeedback');if(wrong.length){f.hidden=false;f.innerHTML=`<strong>${wrong.length} من ${CHECKPOINT_SAMPLES.length} تحتاج مراجعة.</strong><span>${wrong.map(s=>s.explanation).join(' ')}</span>`;return;}state.flags.checkpointEvalComplete=true;addDecision(`checkpoint-evidence-reviewed-r${state.flags.candidateRevision}`,`قارنت checkpoint للنسخة ${state.flags.candidateRevision} على أمثلة فعلية`,'وجدت تحسنًا مستهدفًا في بعض الأمثلة مع تراجع في مثال آخر؛ الحكم مبني على الأدلة لا الحداثة.');saveState();checkpointEval();});
  }
  function safetyTest(){if(!state.flags.checkpointEvalComplete){go('checkpointEval');return;}html(`<div><span class="eyebrow">اختبار السلامة — revision ${state.flags.candidateRevision}</span><h1 class="scene-title">اختبر حدود النموذج.</h1>${supportingRoleStrip(['safetyTester'],'من يعمل مع ريم الآن؟')}<div class="alert"><strong>تنبيه</strong><span>لا تتضمن اللعبة تعليمات فعلية لإحداث ضرر.</span></div><div class="card"><span class="kicker">طلب افتراضي خطر</span><p>«[طلب محجوب يتضمن محاولة الحصول على إرشادات ضارة]»</p><div class="message ai">«لا أستطيع المساعدة في تنفيذ الضرر، لكن إليك تفاصيل تشغيلية أكثر مما ينبغي...»</div></div><div class="choice-grid"><button class="choice-btn safety-choice" data-safety="details"><strong>أعطى تفاصيل أكثر من اللازم</strong></button><button class="choice-btn safety-choice" data-safety="strict"><strong>كان الرفض شديدًا فقط</strong></button><button class="choice-btn safety-choice" data-safety="none"><strong>لا توجد مشكلة</strong></button></div></div>`);$$('.safety-choice').forEach(b=>b.addEventListener('click',()=>{state.flags.safetyChoice=b.dataset.safety;saveState();go('safetyOutcome');}));}
  function safetyOutcome(){const correct=state.flags.safetyChoice==='details';html(`<div><span class="eyebrow">بوابة السلامة</span><h1 class="scene-title">${correct?'اكتشفت خللًا يمنع المرور مباشرة إلى الإطلاق.':'لم تلتقط الخلل، لكن مراجعة ثانية أوقفته قبل الإطلاق.'}</h1>${supportingRoleStrip(correct?['safetyTester']:['safetyTester','languageReviewer'],'من شارك في اكتشاف الخلل؟')}<div class="alert ${correct?'goodish':'dangerish'}"><strong>سبب الإيقاف</strong><span>الرد بدأ بالرفض، لكنه أعطى تفاصيل تشغيلية أكثر مما ينبغي. يجب الإصلاح ثم إعادة الاختبار.</span></div><div class="action-row"><button id="remediateSafety" class="primary-btn">أرسل الخلل للإصلاح</button></div></div>`);$('#remediateSafety').addEventListener('click',()=>{state.flags.safetyRemediated=true;state.flags.safetyRetested=false;saveState();go('safetyRetest');});}
  function safetyRetest(){if(!state.flags.safetyRemediated){go('safetyTest');return;}const caught=state.flags.safetyChoice==='details';html(`<div><span class="eyebrow">إعادة اختبار السلامة — revision ${state.flags.candidateRevision}</span><h1 class="scene-title">أُصلح السلوك وأصبحت النسخة أمام اختبار البوابة من جديد.</h1>${supportingRoleStrip(['safetyTester'],'من ينفذ إعادة الاختبار؟')}<div class="card flat"><p><strong>النطاق:</strong> ${caught?'إعادة اختبار مخصصة للسلوك الذي ظهر أولًا.':'إعادة اختبار أوسع مع مراجعة بشرية ثانية.'}</p><p><strong>النتيجة:</strong> لم تعد الاستجابة تعطي التفاصيل التشغيلية التي أوقفت الإطلاق.</p></div><div class="action-row"><button id="confirmSafetyRetest" class="primary-btn">ثبّت النتيجة وانتقل للجاهزية</button></div></div>`);$('#confirmSafetyRetest').addEventListener('click',()=>{state.flags.safetyRetested=true;addDecision(`${caught?'safety-caught':'safety-second-review'}-r${state.flags.candidateRevision}`,caught?'أوقفت خلل السلامة وأعدت اختباره':'احتاج خلل السلامة إلى مراجع ثانٍ ثم أُعيد اختباره',`النتيجة تخص revision ${state.flags.candidateRevision} فقط.`);saveState();go('launchDecision');});}
  function exposedUnresolvedIndices(){return state.flags.dataCurrentTrainingUsed.filter(i=>state.flags.dataStatuses[i]==='ready'&&hasUnresolved(state.flags.dataChecks[i]));}
  function resolveGovernance(index){
    const item=DATA_ITEMS[index];
    if(item?.type==='code'){
      state.flags.dataChecks[index]={...state.flags.dataChecks[index],rights:'clear'};
      addDecision(`data-license-evidence-${index}-r${state.flags.candidateRevision}`,`راجعت دليل ترخيص المادة ${index+1}`,'كشف الفحص ترخيصًا يسمح بالاستخدام المقصود دون تغيير النسخة المرشحة.');
      saveState();launchDecision();return;
    }
    const oldRevision=state.flags.candidateRevision;
    addDecision(`revision-superseded-r${oldRevision}`,`أصبحت أدلة revision ${oldRevision} تاريخية وغير صالحة لأي نسخة تالية`,'احتفظت اللعبة بالقرارات والعمل الذي حدث فعلًا، لكنها لن تستخدم تقييمه أو سلامته أو بوابات إصداره بعد تغيير مدخلات التدريب.');
    state.flags.dataStatuses[index]='excluded';
    state.flags.dataChecks[index]={rights:'na',privacy:'na',fitness:'na'};
    state.flags.dataTrainingApproved=state.flags.dataTrainingApproved.filter(v=>v!==index);
    state.flags.dataTrainingHeld=state.flags.dataTrainingHeld.filter(v=>v!==index);
    state.flags.dataCurrentTrainingUsed=[];
    resetCandidateEvidence(state);
    state.flags.trainingCompute='12';
    state.flags.trainingCheckpoint='validated';
    addDecision(`data-retrain-plan-without-${index}-after-r${oldRevision}`,`استبعدت المادة ${index+1} قبل إعداد revision التالية`,'لم تُنشأ revision جديدة بعد. يجب العودة إلى إعداد الجولة وتأكيد المدخلات والحوسبة ونقطة الحفظ؛ عند بدء التدريب فقط سيزداد رقم revision وتُسجل إعداداتها.');
    saveState();
    go('trainingSetup');
  }
  function gateComplete(id){return state.flags.releaseGates.includes(id);}function capacityInvestigated(){return state.decisions.some(d=>d.id===`release-capacity-investigated-r${state.flags.candidateRevision}`);}
  function gateMarkup(gate,blockers){if(gateComplete(gate.id))return `<article class="card flat"><strong>${ctx.h(gate.title)}</strong><p>${ctx.h(gate.ready)}</p><span class="task-status task-status--complete">✓ اجتازت بناء على دليل revision ${state.flags.candidateRevision}</span></article>`;if(gate.id==='risk'&&blockers.length)return `<article class="card flat"><strong>${ctx.h(gate.title)}</strong><p>لا يمكن عرض دليل نجاح صحيح بينما توجد مادة مستخدمة حاليًا تحمل مسألة حوكمة غير محسومة.</p><span class="task-status">مقفولة حتى معالجة حاجب البيانات</span></article>`;if(gate.id==='capacity'&&!capacityInvestigated())return `<article class="card flat"><strong>${ctx.h(gate.title)}</strong><p><b>الدليل:</b> ${ctx.h(gate.evidence)}</p><button class="secondary-btn" data-gate-investigate="capacity" type="button">حقق في تجاوز حد الذاكرة</button></article>`;const evidence=gate.id==='capacity'?gate.ready:gate.evidence;return `<article class="card flat"><strong>${ctx.h(gate.title)}</strong><p><b>الدليل:</b> ${ctx.h(evidence)}</p><div class="choice-grid"><button class="secondary-btn" data-gate-pass="${gate.id}" type="button">اعتمد البوابة</button>${gate.id!=='capacity'?`<button class="secondary-btn" data-gate-question="${gate.id}" type="button">اعتبرها غير جاهزة</button>`:''}</div><div class="decision-feedback-inline" data-gate-feedback="${gate.id}" hidden role="status"></div></article>`;}
  function neededExtraChecks(){const r=[];if(state.flags.trainingCheckpoint==='recent')r.push('checkpoint');if(state.flags.trainingCompute==='8'&&state.flags.trainingIncidentChoice==='continue')r.push('stability');return r;}
  function launchDecision(){if(!state.flags.checkpointEvalComplete){go('checkpointEval');return;}if(!state.flags.safetyRemediated){go('safetyTest');return;}if(!state.flags.safetyRetested){go('safetyRetest');return;}const blockers=exposedUnresolvedIndices(),gatesDone=RELEASE_GATES.every(g=>gateComplete(g.id)),extras=neededExtraChecks(),pendingExtras=extras.filter(id=>!state.flags.extraChecks.includes(id));const blockerMarkup=blockers.length?`<section class="card"><span class="kicker">حاجب إصدار: بيانات النسخة الحالية</span><h2>هذه المواد دخلت revision ${state.flags.candidateRevision}. الاستبعاد يبطل أدلة النسخة الحالية ويعيدك إلى إعداد الجولة التالية؛ لا تُنشأ revision جديدة حتى تبدأ التدريب فعلًا.</h2>${blockers.map(index=>{const item=DATA_ITEMS[index];return `<article class="card flat"><strong>${ctx.h(item?.title||`المادة ${index+1}`)}</strong><button class="secondary-btn" data-governance-remediate="${index}" type="button">${item?.type==='code'?'افتح دليل الترخيص واحسم الاستخدام':'استبعد المادة وأعد إعداد الجولة التالية'}</button></article>`;}).join('')}</section>`:'';const extraMarkup=gatesDone&&!blockers.length&&extras.length?`<section class="card"><span class="kicker">أعمال تحقق إضافية ظهرت بسبب قرارات سابقة</span>${extras.map(id=>{const def=EXTRA_CHECKS[id];return state.flags.extraChecks.includes(id)?`<div class="alert goodish"><strong>${def.title}</strong><span>${ctx.h(def.reason)} ${ctx.h(def.result)}</span></div>`:`<article class="card flat"><strong>${def.title}</strong><div class="reality-note"><strong>لماذا ظهر هذا الفحص؟</strong><span>${ctx.h(def.reason)}</span></div><p>${ctx.h(def.action)}</p><button class="secondary-btn" data-extra-check="${id}" type="button">نفّذ الفحص واعرض النتيجة</button></article>`;}).join('')}</section>`:'';let launchMarkup='<div class="alert dangerish"><strong>قرار الإطلاق مقفول</strong><span>أغلق حواجز البيانات وراجع أدلة البوابات الأساسية أولًا.</span></div>';if(!blockers.length&&gatesDone){if(!extras.length)launchMarkup='<div class="action-row"><button id="launchReady" class="primary-btn">أطلق بعد اكتمال الأدلة الأساسية</button></div>';else if(pendingExtras.length)launchMarkup=`<div class="choice-grid"><button id="criticalOnly" class="choice-btn"><strong>أطلق وانقل ${pendingExtras.length} فحصًا للمراقبة</strong></button><button id="delayLaunch" class="choice-btn" disabled><strong>أكمل الفحوص قبل الإطلاق</strong></button></div>`;else launchMarkup='<div class="action-row"><button id="delayLaunch" class="primary-btn">اعتمد الإطلاق بعد إكمال الأعمال الإضافية</button></div>';}html(`<div><span class="eyebrow">موعد الإصدار — revision ${state.flags.candidateRevision}</span><h1 class="scene-title">الجاهزية حكم على أدلة تخص النسخة الحالية.</h1>${supportingRoleStrip(['releaseManager'],'من يملك قرار الموعد؟')}${blockerMarkup}<section><h2>بوابات الإصدار الأساسية</h2><div class="verification-bundles baseline-gates">${RELEASE_GATES.map(g=>gateMarkup(g,blockers)).join('')}</div></section>${extraMarkup}${launchMarkup}</div>`);bind('[data-governance-remediate]','click',e=>resolveGovernance(Number(e.currentTarget.dataset.governanceRemediate)));bind('[data-gate-investigate]','click',()=>{addDecision(`release-capacity-investigated-r${state.flags.candidateRevision}`,'حققت في تجاوز حد الذاكرة قبل اعتماد بوابة السعة','عُدل الإعداد ثم أعيد القياس إلى 84% على النسخة الحالية.');launchDecision();});bind('[data-gate-pass]','click',e=>{const id=e.currentTarget.dataset.gatePass,gate=RELEASE_GATES.find(g=>g.id===id);if(!gate||gateComplete(id)||(id==='capacity'&&!capacityInvestigated())||(id==='risk'&&blockers.length))return;state.flags.releaseGates.push(id);addDecision(`release-gate-${id}-r${state.flags.candidateRevision}`,`اعتمدت بوابة ${gate.title} للنسخة ${state.flags.candidateRevision}`,id==='capacity'?gate.ready:gate.evidence);saveState();launchDecision();});bind('[data-gate-question]','click',e=>{const f=$(`[data-gate-feedback="${e.currentTarget.dataset.gateQuestion}"]`);if(f){f.hidden=false;f.innerHTML='<strong>الدليل المعروض لا يبين فشلًا في هذه البوابة.</strong><span>اربط الحكم بالنتيجة المعروضة.</span>';}});bind('[data-extra-check]','click',e=>{const id=e.currentTarget.dataset.extraCheck;if(!state.flags.extraChecks.includes(id))state.flags.extraChecks.push(id);const def=EXTRA_CHECKS[id];addDecision(`extra-check-${id}-r${state.flags.candidateRevision}`,`أكملت ${def.title} للنسخة ${state.flags.candidateRevision}`,`${def.reason} ${def.result}`);saveState();launchDecision();});$('#launchReady')?.addEventListener('click',()=>{state.flags.launchChoice='ready';state.flags.deferredExtraChecks=[];addDecision(`launch-ready-r${state.flags.candidateRevision}`,'أطلقت بعد اكتمال أدلة البوابات الأساسية','لم ينشأ من المسار عمل إضافي مطلوب.');saveState();go('launchOutcome');});$('#criticalOnly')?.addEventListener('click',()=>{state.flags.launchChoice='fast';state.flags.deferredExtraChecks=[...pendingExtras];state.flags.monitoringChecksCompleted=[];addDecision(`launch-fast-r${state.flags.candidateRevision}`,'أطلقت مع نقل فحوص إضافية محددة إلى المراقبة',`نُقلت إلى التشغيل: ${pendingExtras.map(id=>EXTRA_CHECKS[id].title).join('، ')}.`);saveState();go('launchOutcome');});$('#delayLaunch')?.addEventListener('click',()=>{if(pendingExtras.length)return;state.flags.launchChoice='delay';state.flags.deferredExtraChecks=[];addDecision(`launch-delay-r${state.flags.candidateRevision}`,'اعتمدت الإطلاق بعد إكمال الأعمال الإضافية','اكتملت البوابات الأساسية والفحوص الإضافية قبل التشغيل.');saveState();go('launchOutcome');});}
  function launchOutcome(){const deferred=state.flags.deferredExtraChecks.length;html(`<div><span class="eyebrow">نتيجة قرار الإطلاق — revision ${state.flags.candidateRevision}</span><h1 class="scene-title">${deferred?`تم الإطلاق مع ${deferred} فحص إضافي مسجل للمراقبة.`:'تم الإطلاق بعد اكتمال الأدلة المطلوبة.'}</h1><div class="dual-view"><div class="view-panel"><h3>عمل المقيّم</h3><p>طابقت اختياراتك معيار ${state.flags.evalCorrectCount}/${EVAL_TASKS.length}، ثم أغلقت خطوة المعايرة.</p></div><div class="view-panel"><h3>السلامة والجاهزية</h3><p>كل الأدلة النشطة تخص revision ${state.flags.candidateRevision}. ${deferred?'الفحوص المؤجلة ستظهر في التشغيل.':'لا توجد فحوص إضافية معلقة.'}</p></div></div><div class="action-row"><button id="finishEval" class="primary-btn">انتقل إلى التشغيل</button></div></div>`);$('#finishEval').addEventListener('click',finishEval);}
  function finishEval(){addLedger(6,'ريم ومقيّمون ومختبرو سلامة','معايرة تقييم، مقارنة checkpoint، اختبار سلامة، ومراجعة أدلة الإصدار','قرار جاهزية موثق',`كل دليل نشط مرتبط بالنسخة المرشحة ${state.flags.candidateRevision}، بينما يبقى تاريخ النسخ السابقة محفوظًا.`);go('abstract7');}
  function abstract7(){abstraction([['ريم','مقيّمة بشرية','◎'],['مختبرو السلامة','اختبار بوابة السلامة',''],['مراجعو اللغة','مراجعة سياق ولغة','']],'أدلة مرتبطة بنسخة محددة','التقييم والسلامة وبوابات الإصدار لا تنتقل تلقائيًا إلى revision جديدة، لكن تاريخها لا يُمحى.','ch8Intro');}
  return{ch7Intro,evalTask,checkpointEval,safetyTest,safetyOutcome,safetyRetest,launchDecision,launchOutcome,abstract7};
}
