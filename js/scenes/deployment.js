import { SUPPORT_TASKS } from '../data/content-tasks.js';
import { supportingRoleStrip } from '../components/supporting-role-strip.js';
import { DEPLOY_CAPACITY_LIMITS, FAILOVER_INGRESS_LIMITS, MAX_SURVIVABLE_FAILURES, loadMargins, failoverCase, survivableFailures, hasResilienceResolution, recoveryDispositionComplete } from '../domain/game-rules.js';

const STARTING_LOAD=[20,45,35];
const SITE_NAMES=['أ','ب','ج'];
const INCIDENT_TABS=[['network','الشبكة','الشبكة مستقرة، ولا توجد زيادة واضحة في فقد البيانات.'],['compute','الخوادم','السعة متاحة، لكن بعض العمليات يعاد تشغيلها.'],['model','خدمة النموذج','استهلاك الذاكرة يرتفع تدريجيًا في الإصدار الجديد.']];
const MONITORING_CHECKS={checkpoint:{title:'تحقق موسع لنقطة الحفظ الحديثة',evidence:'شُغلت 12 عينة إضافية أثناء نافذة المراقبة بعد فتح الخدمة.',result:'لم يظهر انحدار جديد يمنع استمرار النسخة الحالية.'},stability:{title:'فحص الاستقرار',evidence:'شُغلت ثلاث جولات مراقبة قصيرة مع تتبع إعادة تشغيل مجموعات الحوسبة.',result:'لم يتكرر عطل الحوسبة في نافذة المراقبة الافتراضية.'}};

export function createDeploymentRoutes(ctx){
  const $=ctx.$,state=ctx.state;
  const {chapterIntro,html,go,bind,tone,saveState,addDecision,addLedger}=ctx;
  const abstraction=(humans,word,line,next)=>ctx.abstraction(humans,word,line,next);
  function ch8Intro(){chapterIntro(7,'deployLoad');}
  function loadValues(){return[0,1,2].map(index=>Number($(`#range${index}`)?.value??0));}
  function savedLoad(){return Array.isArray(state.flags.deployLoad)?state.flags.deployLoad:STARTING_LOAD;}
  function pendingMonitoringChecks(){return state.flags.deferredExtraChecks.filter(id=>!state.flags.monitoringChecksCompleted.includes(id));}
  function supportFeedbackMarkup(){return state.flags.supportFeedbackLabel?`<div class="decision-feedback-inline" role="status"><strong>${ctx.h(state.flags.supportFeedbackLabel)}</strong><span>${ctx.h(state.flags.supportFeedbackDetail)}</span></div>`:'';}

  function deployLoad(){
    if(Array.isArray(state.flags.deployLoad)){go('deployFailover');return;}
    const initial=Array.isArray(state.flags.deployDraftLoad)?state.flags.deployDraftLoad:STARTING_LOAD;
    html(`<div><span class="eyebrow">هانا — مهندسة تشغيل الخدمة</span><h1 class="scene-title">قرار الإصدار معتمد؛ جهّز توزيع التشغيل قبل اختبار الفشل.</h1><div class="reality-note"><strong>المهمة</strong> وزّع 100% ضمن السعات: أ 60%، ب 45%، ج 35%. بعد الاعتماد ستكتشف المرونة الفعلية باختبار خروج كل مركز؛ لا تعرض اللعبة النتيجة القصوى مسبقًا.</div><div class="load-grid">${SITE_NAMES.map((name,index)=>`<div class="load-card"><span>مركز ${name}</span><strong id="load${index}">${initial[index]}%</strong><small>السعة ${DEPLOY_CAPACITY_LIMITS[index]}% — استقبال الطوارئ ${FAILOVER_INGRESS_LIMITS[index]} نقطة</small><input id="range${index}" type="range" min="0" max="100" step="1" value="${initial[index]}" aria-label="حصة مركز البيانات ${name}" /></div>`).join('')}</div><div id="loadFeedback" class="alert" role="status"><strong>المجموع: <span id="loadTotal">${initial.reduce((a,b)=>a+b,0)}</span>%</strong><span>صلاحية الحالة العادية لا تعني تحمل خروج مركز.</span></div><div class="action-row"><button id="testLoad" class="primary-btn">اعتمد التوزيع وانتقل لاختبار المرونة</button></div></div>`);
    const update=()=>{const values=loadValues();values.forEach((v,i)=>{$(`#load${i}`).textContent=`${v}%`;});$('#loadTotal').textContent=values.reduce((a,b)=>a+b,0);};[0,1,2].forEach(i=>$(`#range${i}`)?.addEventListener('input',update));
    $('#testLoad')?.addEventListener('click',()=>{const values=loadValues(),total=values.reduce((a,b)=>a+b,0),feedback=$('#loadFeedback');if(total!==100){feedback.innerHTML=`<strong>المجموع ${total}%</strong><span>يجب أن يساوي 100%.</span>`;tone(170,.08,'square');return;}const overloaded=values.findIndex((v,i)=>v>DEPLOY_CAPACITY_LIMITS[i]);if(overloaded>=0){feedback.innerHTML=`<strong>مركز ${SITE_NAMES[overloaded]} تجاوز سعته.</strong><span>${values[overloaded]}% مقابل ${DEPLOY_CAPACITY_LIMITS[overloaded]}%.</span>`;tone(170,.08,'square');return;}state.flags.deployLoad=values;state.flags.deployDraftLoad=null;state.flags.deployFailoverChecks=[];state.flags.deployResilienceAccepted=false;addDecision(`deploy-capacity-load-${values.join('-')}`,'اعتمدت توزيع التشغيل ضمن السعات',`التوزيع ${values.join(' / ')} صالح للحالة العادية؛ لم تُفتح الخدمة بعد.`);saveState();go('deployFailover');});
  }

  function failoverMarkup(values){const margins=loadMargins(values),checked=state.flags.deployFailoverChecks,resilient=survivableFailures(values);let resolution='';if(checked.length===3){const atMaximum=resilient>=MAX_SURVIVABLE_FAILURES,explanation=`بعد الحالات الثلاث ظهر أن السقف الممكن بهذه السعات وحدود الاستقبال هو ${MAX_SURVIVABLE_FAILURES}/3. خروج أ يترك سعة اسمية قصوى 80%، وخروج ب يترك 95%.`;const retry=atMaximum?'':`<button id="retryLoad" class="choice-btn"><strong>عدّل التوزيع وحاول تحسين النتيجة</strong><small>ستعود بالقيم الحالية نفسها كي تبني على محاولتك.</small></button>`;resolution=`<div class="alert ${atMaximum?'goodish':'dangerish'}"><strong>${atMaximum?'وصلت إلى أفضل مرونة ممكنة بهذه الثوابت.':'ما زالت النتيجة أقل من السقف الممكن.'}</strong><span>${explanation}</span></div><div class="choice-grid">${retry}<button id="finishFailover" class="choice-btn"><strong>${atMaximum?'سجّل القيد البنيوي وتابع':'اقبل فجوة المرونة الحالية وتابع'}</strong><small>${atMaximum?'بلغت أفضل مرونة تسمح بها سعات السيناريو؛ يسجل القيد بدل نسبته إلى اختيار توزيع ضعيف.':'يمكنك تحسين التوزيع أو قبول النتيجة الحالية كفجوة معروفة.'}</small></button></div>`;}return`<div><span class="eyebrow">اختبار المرونة قبل فتح الحركة</span><h1 class="scene-title">اختبر خروج كل مركز بعد اعتماد توزيع التشغيل.</h1><div class="reality-note"><strong>قاعدة الاختبار</strong> كل مركز باقٍ يستطيع استقبال بحد أقصى ${FAILOVER_INGRESS_LIMITS[0]} نقطة إضافية لحظيًا. المتاح فعليًا هو الأصغر بين هامش السعة وحد النقل.</div><div class="hud-grid">${margins.map((m,i)=>`<div class="hud-item"><span>مركز ${SITE_NAMES[i]}: هامش / طوارئ</span><strong>${m} / ${FAILOVER_INGRESS_LIMITS[i]}</strong></div>`).join('')}</div><div class="evidence-results">${[0,1,2].map(index=>{const result=failoverCase(index,values),done=checked.includes(index),receivers=result.perSiteReceivable.map((v,i)=>i===index?null:`${SITE_NAMES[i]}: ${v}`).filter(Boolean).join('، ');return`<article class="card flat"><strong>إذا خرج مركز ${SITE_NAMES[index]}</strong><p>الحمل المزاح: ${result.displaced}. القابل للاستقبال: ${result.spareElsewhere} (${receivers}).</p>${done?`<div class="alert ${result.survivable?'goodish':'dangerish'}"><strong>${result.survivable?'يمكن امتصاص الحمل':'لا يمكن امتصاص الحمل'}</strong></div>`:`<button class="secondary-btn" data-failover-check="${index}">اختبر خروج مركز ${SITE_NAMES[index]}</button>`}</article>`;}).join('')}</div>${checked.length===3?`<div class="stage-output"><strong>${resilient}/3 حالات يمكن امتصاصها</strong>النتيجة ظهرت من الاختبار نفسه.</div>${resolution}`:'<div class="alert"><strong>أكمل الحالات الثلاث</strong><span>لا يمكن وصف المرونة من حالة واحدة.</span></div>'}</div>`;}

  function deployFailover(){if(!Array.isArray(state.flags.deployLoad)){go('deployLoad');return;}const values=savedLoad();html(failoverMarkup(values));bind('[data-failover-check]','click',event=>{const index=Number(event.currentTarget.dataset.failoverCheck);if(!state.flags.deployFailoverChecks.includes(index))state.flags.deployFailoverChecks.push(index);saveState();deployFailover();});$('#retryLoad')?.addEventListener('click',()=>{state.flags.deployDraftLoad=[...values];state.flags.deployLoad=null;state.flags.deployFailoverChecks=[];state.flags.deployResilienceAccepted=false;saveState();go('deployLoad');});$('#finishFailover')?.addEventListener('click',()=>{if(state.flags.deployFailoverChecks.length!==3)return;const resilient=survivableFailures(values),atMaximum=resilient>=MAX_SURVIVABLE_FAILURES;state.flags.deployResilienceAccepted=resilient<3;addDecision(`deploy-resilience-${values.join('-')}`,atMaximum?'اختبرت المرونة وسجلت القيد البنيوي قبل فتح الخدمة':'اختبرت المرونة وقبلت الفجوة الحالية قبل فتح الخدمة',`التوزيع ${values.join(' / ')} يتحمل ${resilient}/3 حالات كاملة.`);saveState();go('deployGoLive');});}

  function deployGoLive(){if(!hasResilienceResolution(state)){go('deployFailover');return;}if(state.flags.deployTrafficOpen){go(state.flags.deferredExtraChecks.length?'deployMonitoring':'deployIncident');return;}html(`<div><span class="eyebrow">بوابة فتح الخدمة</span><h1 class="scene-title">النسخة معتمدة والتوزيع مختبر، لكن حركة المستخدمين لم تُفتح بعد.</h1><div class="reality-note"><strong>الفرق بين القرار والتنفيذ</strong> اعتماد الإصدار واختبار المرونة لا يجعلان الخدمة متاحة تلقائيًا. افتح الحركة الآن كفعل تشغيلي مستقل.</div>${state.flags.deferredExtraChecks.length?`<div class="alert dangerish"><strong>${state.flags.deferredExtraChecks.length} فحص إضافي مؤجل</strong><span>سيبدأ كدين تحقق ظاهر فور فتح الخدمة.</span></div>`:'<div class="alert goodish"><strong>لا يوجد دين تحقق مؤجل</strong><span>يمكن فتح الحركة ثم متابعة التشغيل.</span></div>'}<div class="action-row"><button id="openTraffic" class="primary-btn">افتح حركة المستخدمين</button></div></div>`);$('#openTraffic').addEventListener('click',()=>{state.flags.deployTrafficOpen=true;addDecision(`deploy-traffic-open-r${state.flags.candidateRevision}`,'فتحت حركة المستخدمين بعد اختبار التشغيل','أصبح فتح الخدمة فعلًا مستقلًا عن قرار الإصدار واختبار المرونة.');saveState();go(state.flags.deferredExtraChecks.length?'deployMonitoring':'deployIncident');});}

  function deployMonitoring(){if(!state.flags.deployTrafficOpen){go('deployGoLive');return;}if(!state.flags.deferredExtraChecks.length){go('deployIncident');return;}const pending=pendingMonitoringChecks();if(!state.flags.deployMonitoringOpened){html(`<div><span class="eyebrow">الخدمة متاحة الآن</span><h1 class="scene-title">ابدأ نافذة المراقبة مع دين التحقق المسجل.</h1><div class="alert dangerish"><strong>${pending.length} فحص إضافي مفتوح</strong><span>فتح الحركة حدث في الخطوة السابقة؛ الآن تبدأ مراقبة الدين أثناء التشغيل.</span></div><div class="action-row"><button id="openMonitoringWindow" class="primary-btn">ابدأ نافذة المراقبة</button></div></div>`);$('#openMonitoringWindow').addEventListener('click',()=>{state.flags.deployMonitoringOpened=true;addDecision(`deploy-monitoring-window-r${state.flags.candidateRevision}`,'بدأت نافذة المراقبة بعد فتح الخدمة','الفحوص المؤجلة بقيت ظاهرة كعمل مفتوح.');saveState();deployMonitoring();});return;}html(`<div><span class="eyebrow">نافذة المراقبة</span><h1 class="scene-title">أغلق دين التحقق بينما الخدمة تعمل.</h1><div class="evidence-results">${state.flags.deferredExtraChecks.map(id=>{const def=MONITORING_CHECKS[id],done=state.flags.monitoringChecksCompleted.includes(id);return`<article class="card flat"><strong>${ctx.h(def.title)}</strong><p>${ctx.h(def.evidence)}</p>${done?`<div class="alert goodish"><strong>اكتمل</strong><span>${ctx.h(def.result)}</span></div>`:`<button class="secondary-btn" data-monitoring-check="${id}">نفّذ الفحص وسجل النتيجة</button>`}</article>`;}).join('')}</div>${pending.length?'<div class="alert"><strong>الدين ما زال مفتوحًا</strong><span>استمرار الخدمة لا يحول الفحص إلى مكتمل.</span></div>':'<div class="action-row"><button id="finishMonitoring" class="primary-btn">أغلق نافذة المراقبة وتابع التشغيل</button></div>'}</div>`);bind('[data-monitoring-check]','click',event=>{const id=event.currentTarget.dataset.monitoringCheck;if(!state.flags.monitoringChecksCompleted.includes(id))state.flags.monitoringChecksCompleted.push(id);const def=MONITORING_CHECKS[id];addDecision(`monitoring-check-${id}-r${state.flags.candidateRevision}`,`أغلقت ${def.title} أثناء التشغيل`,def.result);saveState();deployMonitoring();});$('#finishMonitoring')?.addEventListener('click',()=>go('deployIncident'));}

  function diagnosisSummary(){return INCIDENT_TABS.filter(([id])=>state.flags.deployTabs.includes(id)).map(([id,label,text])=>`<div class="diagnosis-row diagnosis-row--${id}"><strong>${label}</strong><span>${text}</span></div>`).join('');}
  function deployIncident(){const values=savedLoad(),resilient=survivableFailures(values);if(!state.flags.deployTrafficOpen){go('deployGoLive');return;}if(pendingMonitoringChecks().length){go('deployMonitoring');return;}const complete=state.flags.deployTabs.length===INCIDENT_TABS.length;html(`<div><span class="eyebrow">عطل جديد أثناء التشغيل</span><h1 class="scene-title">ارتفعت الأخطاء بعد إصدار جديد.</h1>${supportingRoleStrip(['operationsTeam'],'من يعمل مع هانا أثناء التشخيص؟')}<div class="reality-note"><strong>سياق المرونة</strong> توزيعك ${values.join(' / ')}% تحمل ${resilient}/3 حالات خروج. هذا لا يثبت سبب الحادث.</div><div class="incident-tabs">${INCIDENT_TABS.map(([id,label])=>state.flags.deployTabs.includes(id)?`<span class="incident-tab incident-tab--done">✓ ${label}</span>`:`<button data-tab="${id}">${label}</button>`).join('')}</div><div class="diagnosis-stack">${state.flags.deployTabs.length?diagnosisSummary():'<div class="card flat">ابدأ بفحص الشبكة أو الخوادم أو خدمة النموذج.</div>'}</div>${complete?'<div class="alert"><strong>الاستنتاج الحالي</strong><span>الشبكة مستقرة والسعة متاحة، بينما يرتفع استهلاك الذاكرة في الإصدار الجديد؛ لذلك يصبح الإصدار المشتبه الرئيسي.</span></div><div class="choice-grid"><button id="restartInst" class="choice-btn"><strong>أعد تشغيل الوحدات المتأثرة</strong><small>عودة أسرع، لكنها معالجة مؤقتة.</small></button><button id="rollback" class="choice-btn"><strong>ارجع إلى الإصدار السابق</strong><small>استعادة تستهدف المشتبه الرئيسي.</small></button></div>':''}</div>`);bind('[data-tab]','click',event=>{if(!state.flags.deployTabs.includes(event.currentTarget.dataset.tab))state.flags.deployTabs.push(event.currentTarget.dataset.tab);saveState();deployIncident();});$('#restartInst')?.addEventListener('click',()=>{state.flags.deployRecovery='restart';state.flags.deployRecoveryVerifiedFor=null;state.flags.deployRecoveryDisposition=null;addDecision('deploy-restart','أعدت تشغيل الوحدات المتأثرة','استعادة سريعة لا تحسم الإصدار المشتبه به.');saveState();go('onCall');});$('#rollback')?.addEventListener('click',()=>{state.flags.deployRecovery='rollback';state.flags.deployRecoveryVerifiedFor=null;state.flags.deployRecoveryDisposition=null;addDecision('deploy-rollback','رجعت إلى الإصدار السابق','أزلت الإصدار المشتبه به؛ بقي التحقق الصحي.');saveState();go('onCall');});}

  function onCall(){const recovery=state.flags.deployRecovery;if(recovery===null){go('deployIncident');return;}const restarted=recovery==='restart';if(state.flags.deployRecoveryVerifiedFor!==recovery){html(`<div><span class="eyebrow">تحقق الاستعادة</span><h1 class="scene-title">إجراء الاستعادة نُفذ، لكن لا تعلن عودة الخدمة قبل القياس.</h1>${supportingRoleStrip(['operationsTeam'],'من يتحقق من الاستعادة؟')}<div class="hud-grid"><div class="hud-item"><span>معدل الأخطاء</span><strong>عاد للنطاق</strong></div><div class="hud-item"><span>الذاكرة</span><strong>${restarted?'مستقرة الآن مع بقاء الإصدار الحالي':'مستقرة بعد العودة للإصدار السابق'}</strong></div><div class="hud-item"><span>طلب تجريبي</span><strong>نجح</strong></div></div><div class="action-row"><button id="verifyRecovery" class="primary-btn">ثبّت نتيجة التحقق</button></div></div>`);$('#verifyRecovery').addEventListener('click',()=>{state.flags.deployRecoveryVerifiedFor=recovery;if(!restarted)state.flags.deployRecoveryDisposition='cleared';addDecision(`deploy-recovery-verified-${recovery}`,'تحققت من عودة الخدمة بعد إجراء الاستعادة',restarted?'الخدمة عادت لكن الإصدار المشتبه به ما زال موجودًا.':'نجح التحقق على الإصدار السابق.');saveState();onCall();});return;}if(restarted&&state.flags.deployRecoveryDisposition===null){html(`<div><span class="eyebrow">الخدمة تعافت مؤقتًا</span><h1 class="scene-title">إعادة التشغيل نجحت، لكن السبب المشتبه به لم يُزل.</h1>${supportingRoleStrip(['operationsTeam'],'من يقرر مصير الخطر المتبقي؟')}<div class="choice-grid"><button id="rollbackAfterRestart" class="choice-btn"><strong>ارجع إلى الإصدار السابق</strong><small>ستحتاج تحققًا جديدًا.</small></button><button id="monitorRelease" class="choice-btn"><strong>أبق الإصدار تحت المراقبة</strong><small>سجل دينًا تشغيليًا صريحًا.</small></button></div></div>`);$('#rollbackAfterRestart').addEventListener('click',()=>{state.flags.deployRecovery='rollback';state.flags.deployRecoveryVerifiedFor=null;state.flags.deployRecoveryDisposition=null;addDecision('deploy-rollback-after-restart','رجعت إلى الإصدار السابق بعد استعادة مؤقتة','أزيل الإصدار المشتبه به وبدأ تحقق جديد.');saveState();onCall();});$('#monitorRelease').addEventListener('click',()=>{state.flags.deployRecoveryDisposition='monitor';addDecision('deploy-residual-risk-monitor','قبلت دينًا تشغيليًا بعد الاستعادة','بقي الإصدار المشتبه به تحت مراقبة صريحة.');saveState();onCall();});return;}if(!recoveryDispositionComplete(state)){go('deployIncident');return;}const monitored=state.flags.deployRecoveryDisposition==='monitor';html(`<div><span class="eyebrow">بعد التحقق من الاستعادة</span><h1 class="scene-title">الخدمة عادت، لكن أثر الحادث ما زال يصل إلى المستخدمين.</h1>${supportingRoleStrip(['affectedUser'],'من يظهر في الواجهة الآن؟')}<div class="card"><p>${monitored?'أعيد تشغيل الوحدات وتحققت العودة، وبقي الإصدار المشتبه به دينًا تشغيليًا تحت المراقبة.':'تم الرجوع إلى الإصدار السابق والتحقق من العودة.'}</p></div><div class="action-row"><button id="toSupport" class="primary-btn">انتقل إلى بلاغات الحادث</button></div></div>`);$('#toSupport').addEventListener('click',()=>go('supportTask'));}

  function supportTask(){const index=state.flags.supportIndex;if(!recoveryDispositionComplete(state)){go('onCall');return;}if(index>=SUPPORT_TASKS.length){go('deployEnd');return;}const task=SUPPORT_TASKS[index],values=savedLoad();html(`<div><span class="eyebrow">سامر — دعم المستخدمين</span><h1 class="scene-title">بلاغ مرتبط بالحادث ${index+1}/${SUPPORT_TASKS.length}</h1>${supportingRoleStrip(['affectedUser'],'صاحب البلاغ')}${supportFeedbackMarkup()}<div class="reality-note"><strong>سياق التشغيل</strong> التوزيع أثناء الحادث: ${values.join(' / ')}%.</div><div class="card"><div class="message user">${ctx.h(task.q)}</div><div class="choice-grid"><button id="supportInvestigate" class="choice-btn"><strong>${ctx.h(task.aTitle)}</strong><small>${ctx.h(task.aDetail)}</small></button><button id="supportFast" class="choice-btn"><strong>${ctx.h(task.bTitle)}</strong><small>${ctx.h(task.bDetail)}</small></button></div></div></div>`);$('#supportInvestigate')?.addEventListener('click',()=>{addDecision(`support-evidence-${index}`,`حافظت على أدلة البلاغ ${index+1}`,'ربطت تجربة المستخدم ببيانات الحادث.');state.flags.supportFeedbackLabel='احتفظ الفريق بسياق تشخيصي أفضل';state.flags.supportFeedbackDetail='استغرقت المعالجة وقتًا أطول قليلًا.';state.flags.supportIndex++;saveState();supportTask();});$('#supportFast')?.addEventListener('click',()=>{addDecision(`support-fast-${index}`,`قدمت استعادة أسرع للبلاغ ${index+1}`,'الإجراء أسرع لكنه يحفظ معلومات أقل للتحقيق.');state.flags.supportFeedbackLabel='حصل المستخدم على مسار استعادة أسرع';state.flags.supportFeedbackDetail='تحسنت السرعة المباشرة مع أدلة أقل.';state.flags.supportIndex++;saveState();supportTask();});}
  function deployEnd(){const values=savedLoad(),resilient=survivableFailures(values),monitored=state.flags.deployRecoveryDisposition==='monitor';addLedger(7,'هانا وسامر وفرق العمليات','توزيع حمل، اختبار مرونة، فتح حركة، مراقبة، تشخيص، استعادة، تحقق ودعم','الخدمة متاحة للمستخدمين',`تحمل التوزيع ${resilient}/3 حالات؛ أُغلقت الفحوص المؤجلة ${state.flags.monitoringChecksCompleted.length}/${state.flags.deferredExtraChecks.length}.${monitored?' بقي دين تشغيلي مفتوحًا.':' أُزيل الإصدار المشتبه به.'}`);html(`<div><span class="eyebrow">نهاية مرحلة التشغيل</span><h1 class="scene-title">اكتملت مراحل اللعب الثماني.</h1>${supportFeedbackMarkup()}<div class="stage-output"><strong>الخدمة متاحة بعد فتح صريح واستعادة متحققة</strong>خلف كلمة «متاحة» توجد سعات واختبارات وفتح حركة ومراقبة وتشخيص واستعادة ودعم.</div><div class="hud-grid"><div class="hud-item"><span>توزيع الحمل</span><strong>${values.join(' / ')}%</strong></div><div class="hud-item"><span>حالات الخروج</span><strong>${resilient}/3</strong></div><div class="hud-item"><span>الفحوص المؤجلة</span><strong>${state.flags.monitoringChecksCompleted.length}/${state.flags.deferredExtraChecks.length}</strong></div><div class="hud-item"><span>السبب المشتبه به</span><strong>${monitored?'مفتوح تحت المراقبة':'أزيل من الخدمة'}</strong></div></div>${monitored?'<div class="alert dangerish"><strong>دين تشغيلي باقٍ</strong><span>الاستعادة لم تثبت معالجة السبب الجذري.</span></div>':''}<div class="action-row"><button id="uptimeAbstract" class="primary-btn">ركّب الصورة الكاملة</button></div></div>`);$('#uptimeAbstract')?.addEventListener('click',()=>go('abstract8'));}
  function abstract8(){abstraction([['هانا','مهندسة تشغيل','◉'],['سامر','دعم المستخدمين','◌'],['فرق المناوبة والصيانة','','◇']],'الخدمة متاحة','يختصر النظام التوزيع والمرونة وفتح الحركة والمراقبة والتشخيص والاستعادة والتحقق والدعم في حالة بسيطة يراها المستخدم: «الخدمة متاحة».','pipelineAssemble');}
  return{ch8Intro,deployLoad,deployFailover,deployGoLive,deployMonitoring,deployIncident,onCall,supportTask,deployEnd,abstract8};
}
