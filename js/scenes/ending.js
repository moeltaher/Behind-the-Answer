import { CHAPTERS } from '../data/chapters.js';
import { DEMO_PROMPT } from '../data/story.js';
import { storyCharacters } from '../data/characters.js';
import { supportingActor } from '../data/supporting-actors.js';
import { characterGrid } from '../components/character-card.js';
import { EVAL_TASKS, DATA_ITEMS } from '../data/content-tasks.js';
import { hasUnresolved } from '../domain/game-rules.js';

const PIPELINE_STEPS=CHAPTERS.map(chapter=>chapter.pipelineLabel);
const BASE_SUPPORTING_IDS=['transportTeam','maintenance','qualityInspector','cleaner','securityWorker','coolingTech','electricalEngineer','cableTech','networkOperator','contentCreators','rightsReviewer','dataReviewer','infraTeam','safetyTester','releaseManager','operationsTeam','affectedUser'];
const FIXED_ANSWER='أعتذر عن التأخر في تسليم العمل. واجهت ظرفًا أدى إلى تأخير الإنجاز، وأعمل حاليًا على استكماله في أقرب وقت. أشكرك على تفهمك.';
const TRANSFER_TASKS=[
  ['weights','أوزان النموذج الناتجة عن التدريب والموجودة قبل الطلب الحالي','build'],
  ['retrieval','في خدمة تستخدم الاسترجاع المعزز بالتوليد: استرجاع مستندات مرتبطة بسؤال المستخدم الحالي','request'],
  ['inference','تنفيذ الاستدلال لإنتاج نتيجة الطلب الحالي','request'],
  ['monitoring','مراقبة الأخطاء والسعة والحوادث أثناء تشغيل الخدمة','continuous'],
  ['maintenance','صيانة الخوادم والتبريد والشبكات التي تبقي البنية متاحة','continuous']
];
const TRANSFER_LABELS={build:'بُني قبل الطلب',request:'يحدث مع الطلب الحالي',continuous:'تشغيل مستمر'};
function decisionRows(decisions,h){return decisions.length?decisions.map(decision=>`<div class="decision-row"><strong>${h(decision.label)}</strong><div class="small muted">${h(decision.effectText)}</div></div>`).join(''):'<p class="muted">لا توجد قرارات مسجلة في هذا المحور.</p>';}
function decisionCategory(id){if(id.startsWith('mine-')||id.startsWith('annotation-'))return'labor';if(id.startsWith('factory-')||id.startsWith('data-')||id.startsWith('dc-'))return'materials';if(id.startsWith('training-')||id.startsWith('train-')||id.startsWith('evaluator-')||id.startsWith('checkpoint-')||id.startsWith('safety-')||id.startsWith('release-')||id.startsWith('extra-check-')||id.startsWith('launch-')||id.startsWith('revision-'))return'development';if(id.startsWith('monitoring-')||id.startsWith('deploy-')||id.startsWith('support-'))return'operations';return'other';}
function categorizedDecisions(state){const groups={labor:[],materials:[],development:[],operations:[],other:[]};state.decisions.forEach(decision=>groups[decisionCategory(decision.id)].push(decision));return groups;}
function priorityDecision(decisions,priorities){const newest=[...decisions].reverse();for(const priority of priorities){const match=newest.find(decision=>priority.endsWith('*')?decision.id.startsWith(priority.slice(0,-1)):decision.id===priority);if(match)return match;}return newest[0]||null;}
function highlightCard(title,decision,fallback,h){return decision?`<article class="journey-highlight"><span>${h(title)}</span><strong>${h(decision.label)}</strong><small>${h(decision.effectText)}</small></article>`:`<article class="journey-highlight"><span>${h(title)}</span><strong>${h(fallback)}</strong></article>`;}
function dataLineageSummary(state){const current=new Set(state.flags.dataCurrentTrainingUsed),historical=new Set(state.flags.dataTrainingUsed),held=new Set(state.flags.dataTrainingHeld);const rows=state.flags.dataStatuses.map((status,index)=>{let lineage='لم تدخل أي نسخة مرشحة';if(current.has(index))lineage=`ضمن النسخة المرشحة ${state.flags.candidateRevision} الحالية`;else if(historical.has(index))lineage='استُخدمت تاريخيًا ثم خرجت من النسخة الحالية';else if(held.has(index))lineage='موقوفة قبل التدريب الإضافي';else if(status==='pending')lineage='معلقة في مسار العمل ولم تدخل التدريب';else if(status==='excluded')lineage='مستبعدة قبل التدريب';const issue=status==='ready'&&hasUnresolved(state.flags.dataChecks[index])?'مسألة غير محسومة':'لا حاجب حوكمة حالي معروض';return{index,lineage,issue};});return{rows,currentCount:current.size,historicalOnly:[...historical].filter(index=>!current.has(index)).length,heldCount:held.size,pendingCount:state.flags.dataStatuses.filter(status=>status==='pending').length};}
function representedSupportingActors(state){const ids=[...BASE_SUPPORTING_IDS];if(state.flags.safetyChoice!=='details')ids.push('languageReviewer');return[...new Set(ids)].map(supportingActor).filter(Boolean);}
function deliveryState(state){if(state.flags.deployRecoveryDisposition==='monitor')return{label:'وصل بعد استعادة تحققت منها الفرق',status:'إعادة التشغيل أعادت الخدمة بعد تحقق صحي، لكن الإصدار المشتبه به بقي دينًا تشغيليًا مفتوحًا تحت المراقبة.'};return{label:'وصل بعد العودة إلى الإصدار السابق',status:'تحققت الفرق من معدل الأخطاء والذاكرة وطلب تجريبي بعد الرجوع إلى الإصدار السابق.'};}

export function createEndingRoutes(ctx){
const h=ctx.h,$=ctx.$,state=ctx.state,{html,go,renderLedger,resetGame,saveState}=ctx,ledgerDialog=ctx.ledgerDialog,people=storyCharacters();
function pipelineAssemble(){html(`<div class="centered epilogue-screen"><span class="eyebrow">اكتملت مراحل اللعب الثماني</span><h1 class="scene-title">افصل الآن بين ترتيب اللعب وبنية النظام.</h1><p class="scene-subtitle">المراحل الثماني كانت ترتيبًا تعليميًا، لا مخططًا هندسيًا يقول إن الأعمال تقع مرة واحدة وبالترتيب نفسه.</p><div class="system-map"><section class="view-panel"><h2>مسار مادي طويل الأجل</h2><div class="view-list"><span>مواد ومعالجة</span><span>مكونات وأجهزة</span><span>خوادم ومراكز بيانات</span></div></section><section class="view-panel"><h2>دورة تطوير متكررة</h2><div class="view-list"><span>بيانات</span><span>↔ تصنيف ومراجعة</span><span>↔ تدريب إضافي</span><span>↔ تقييم</span></div></section><section class="view-panel"><h2>تشغيل مستمر</h2><div class="view-list"><span>إصدار</span><span>فتح حركة</span><span>استخدام وتشغيل ودعم</span><span>↺ معلومات تعود للتطوير</span></div></section></div><details class="transition-details"><summary>ترتيب المراحل الذي لعبته</summary><div class="view-list abstraction-summary">${PIPELINE_STEPS.map(step=>`<span>${h(step)}</span>`).join('')}</div><p class="small muted">هذا ترتيب اللعبة فقط.</p></details><div class="reality-note reality-note--wide"><strong>الدقة الزمنية</strong> عندما ترسل طلبًا الآن لا يبدأ التعدين والتصنيع والتدريب من جديد. هذه الأعمال بنت النظام سابقًا؛ الطلب الحالي يستخدم بنية ونموذجًا قائمين داخل تشغيل مستمر.</div><div class="action-row center"><button id="backPrompt" class="primary-btn">ارجع إلى الإجابة التي بدأت منها</button></div></div>`);$('#backPrompt').addEventListener('click',()=>go('finalAnswer'));}
function finalAnswer(){const delivery=deliveryState(state);html(`<div class="chat-shell epilogue-chat"><div class="chat-logo">ن</div><div class="message user">${h(DEMO_PROMPT)}</div><div class="message ai"><strong>الإجابة:</strong><br>${h(FIXED_ANSWER)}</div><div class="delivery-state"><strong>${h(delivery.label)}</strong><span>${h(delivery.status)}</span></div><div class="dual-view"><div class="view-panel"><h2>ما بُني قبل طلبك</h2><div class="view-list"><span>الأجهزة</span><span>مراكز البيانات</span><span>مواد البيانات</span><span>التطوير والتقييم</span></div></div><div class="view-panel"><h2>ما يحدث عند الضغط على «إرسال»</h2><div class="view-list"><span>يصل الطلب إلى الخدمة المفتوحة</span><span>تشغله البنية القائمة</span><span>تعود النتيجة أو تفشل المحاولة</span></div></div></div><div class="reality-note"><strong>وقد يحدث وقت الطلب بحسب تصميم المنتج</strong><span>توجيه الطلب، استرجاع مستندات، فحوص محتوى، استدعاء أدوات، تخزين مؤقت أو تسجيل تشغيلي.</span></div><div class="reality-note"><strong>ما الذي لم تدّعه اللعبة؟</strong> صياغة رسالة الاعتذار ثابتة؛ قرارات التعدين والتصنيع لا تجعل النص نفسه أفضل لغويًا. أثر التشغيل يظهر في حالة الوصول والاستعادة.</div><div class="action-row"><button id="transferFromAnswer" class="primary-btn">اختبر نقل الفكرة إلى منتج آخر</button></div></div>`);$('#transferFromAnswer').addEventListener('click',()=>go('transferChallenge'));}
function transferChallenge(){if(state.flags.transferChoice==='build-use'){html(`<div><span class="eyebrow">تحدي انتقال التعلم</span><h1 class="scene-title">طبّقت الفكرة على خدمة مختلفة.</h1><div class="alert goodish"><strong>التمييز الأساسي محفوظ</strong><span>فصلت بين ما بُني تاريخيًا، وما يحدث بسبب الطلب الحالي، وما يستمر أثناء التشغيل.</span></div><div class="action-row"><button id="transferContinue" class="primary-btn">اعرض نتيجة رحلتك</button></div></div>`);$('#transferContinue').addEventListener('click',()=>go('results'));return;}html(`<div><span class="eyebrow">تحدي انتقال التعلم</span><h1 class="scene-title">غيّر المنتج: صنّف العمل في خدمة تستخدم الاسترجاع المعزز بالتوليد.</h1><p class="scene-subtitle">ضع كل عنصر في زمنه الأساسي. الهدف نقل المعيار السببي، لا حفظ ترتيب المراحل.</p><div class="card">${TRANSFER_TASKS.map(([id,label])=>`<label class="form-row"><span>${h(label)}</span><select data-transfer-item="${id}"><option value="">اختر…</option><option value="build">بُني قبل الطلب</option><option value="request">يحدث مع الطلب الحالي</option><option value="continuous">تشغيل مستمر</option></select></label>`).join('')}<div class="decision-feedback-inline" id="transferFeedback" hidden role="status"></div><div class="action-row"><button id="transferSubmit" class="primary-btn">تحقق من التصنيف</button></div></div></div>`);$('#transferSubmit').addEventListener('click',()=>{const answers=Object.fromEntries([...document.querySelectorAll('[data-transfer-item]')].map(select=>[select.dataset.transferItem,select.value])),wrong=TRANSFER_TASKS.filter(([id,,correct])=>answers[id]!==correct),feedback=$('#transferFeedback');if(wrong.length){feedback.hidden=false;feedback.innerHTML=`<strong>${wrong.length} عناصر تحتاج إعادة تصنيف.</strong><span>${wrong.map(([id,label,correct])=>`${label}: «${TRANSFER_LABELS[correct]}» لأن ${id==='weights'?'الأوزان موجودة قبل الطلب الحالي':id==='retrieval'||id==='inference'?'التنفيذ ينشأ بسبب الطلب الحالي':'العمل يستمر خارج طلب بعينه'}.`).join(' ')}</span>`;return;}state.flags.transferChoice='build-use';saveState();transferChallenge();});}
function results(){const groups=categorizedDecisions(state),data=dataLineageSummary(state),supportingPeople=representedSupportingActors(state),safety=state.flags.safetyChoice==='details'?'اكتشفت الخلل مبكرًا ثم عُدلت سياسة الاستجابة وأعيد الاختبار.':'احتاج الخلل إلى مراجعة ثانية ثم عُدلت السياسة وأعيد الاختبار.',evaluation=`طابقت معيار السيناريو في ${state.flags.evalCorrectCount} من ${EVAL_TASKS.length} مهام، ثم أغلقت المعايرة.`,laborHighlight=priorityDecision(groups.labor,['mine-forced-inspection','mine-stop','mine-continue','annotation-appeal','annotation-noappeal','annotation-break','annotation-no-break']),materialHighlight=priorityDecision(groups.materials,['data-redaction-retrain-*','data-retrain-plan-*','data-license-evidence-*','data-pii-keep-after-review','data-pii-redact-after-review','dc-cooling-close','factory-final-inspection','factory-maintenance-verified','factory-debt-carried','factory-production-completed','data-*']),trainingHighlight=priorityDecision(groups.development,['launch-delay*','launch-fast*','launch-ready*','release-gate-*','checkpoint-evidence-reviewed*','safety-second-review*','safety-caught*','train-recovery*','train-continue*']),operationsHighlight=priorityDecision(groups.operations,['deploy-traffic-open*','deploy-residual-risk-monitor','deploy-recovery-verified-*','deploy-rollback-after-restart','deploy-resilience-*','monitoring-check-*','deploy-rollback','deploy-restart','support-*']),lineageRows=data.rows.map(row=>`<article class="card flat"><strong>${h(DATA_ITEMS[row.index]?.title||`المادة ${row.index+1}`)}</strong><p>${h(row.lineage)}</p><p class="small muted">${h(row.issue)}</p></article>`).join(''),otherSection=groups.other.length?`<section class="evidence-card"><h2>قرارات أخرى</h2><div class="decision-list">${decisionRows(groups.other,h)}</div></section>`:'',operationalDebt=state.flags.deployRecoveryDisposition==='monitor'?'<div class="alert dangerish"><strong>دين تشغيلي مفتوح</strong><span>الخدمة تعافت، لكن الإصدار المشتبه به بقي تحت المراقبة.</span></div>':'';html(`<div><span class="eyebrow">نتيجة رحلتك</span><h1 class="display-title">الواجهة هي نهاية السلسلة، وليست بدايتها.</h1><p class="scene-subtitle">الإجابة تظهر في لحظة، لكن العمل والبنية والقرارات التي جعلتها ممكنة لا تظهر معها.</p>${operationalDebt}<div class="journey-highlights">${highlightCard('العمل والوقت',laborHighlight,'لا قرار بارز مسجل',h)}${highlightCard('المواد والبيانات',materialHighlight,`${data.currentCount} مواد في النسخة الحالية`,h)}${highlightCard('التدريب والجاهزية',trainingHighlight,`النسخة ${state.flags.candidateRevision} اجتازت الأدلة`,h)}${highlightCard('التشغيل والدعم',operationsHighlight,'الخدمة عادت بعد تحقق الاستعادة',h)}</div><div class="people-wall">${characterGrid(people)}</div><details class="secondary-labor-details"><summary>أدوار بشرية أخرى تمثلها السلسلة (${supportingPeople.length})</summary><p class="small muted">تشمل القائمة الأدوار التي شاركت في المراجعات والحوكمة والتشغيل، مع إضافة مراجع اللغة فقط عندما يستدعيه مسار السلامة.</p><div class="people-wall supporting-people-wall">${characterGrid(supportingPeople)}</div></details><section class="evidence-card"><h2>سلسلة استخدام البيانات عبر النسخ</h2><div class="hud-grid"><div class="hud-item"><span>في النسخة الحالية</span><strong>${data.currentCount}</strong></div><div class="hud-item"><span>استُخدمت تاريخيًا فقط</span><strong>${data.historicalOnly}</strong></div><div class="hud-item"><span>موقوفة قبل التدريب</span><strong>${data.heldCount}</strong></div><div class="hud-item"><span>معلقة في المسار</span><strong>${data.pendingCount}</strong></div></div><div class="evidence-results">${lineageRows}</div></section><div class="dual-view result-core"><div class="view-panel"><h2>عملية التقييم البشري</h2><p>${h(evaluation)}</p></div><div class="view-panel"><h2>اختبار السلامة</h2><p>${h(safety)}</p></div></div><details class="full-evidence-details" data-decision-count="${state.decisions.length}"><summary>عرض السجل الكامل لكل قرارات الرحلة (${state.decisions.length})</summary><div class="evidence-results"><section class="evidence-card"><h2>العمل والوقت</h2>${decisionRows(groups.labor,h)}</section><section class="evidence-card"><h2>المواد والبيانات والبنية</h2>${decisionRows(groups.materials,h)}</section><section class="evidence-card"><h2>التدريب والتحقق والسلامة</h2>${decisionRows(groups.development,h)}</section><section class="evidence-card"><h2>التشغيل ودعم المستخدم</h2>${decisionRows(groups.operations,h)}</section>${otherSection}</div></details><div class="action-row"><button id="resultsLedger" class="secondary-btn">عرض دفتر السلسلة</button><button id="restartFromResults" class="primary-btn">ابدأ رحلة جديدة</button></div></div>`);$('#resultsLedger').addEventListener('click',()=>{renderLedger();ledgerDialog.showModal();});$('#restartFromResults').addEventListener('click',()=>resetGame(true));}
return{pipelineAssemble,finalAnswer,transferChallenge,results};
}