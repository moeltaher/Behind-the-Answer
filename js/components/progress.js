export function setChapter(ctx,index){const {state,progressEl,ledgerBtn,persistentFooter,chapterLabel,chapterTitle,progressFill,chapters,saveState}=ctx;state.chapter=index;if(index>=0){progressEl.hidden=false;ledgerBtn.hidden=false;persistentFooter.hidden=false;chapterLabel.textContent=`المرحلة ${index+1} من 9`;chapterTitle.textContent=chapters[index]?.title||'';progressFill.style.width=`${((index+1)/9)*100}%`;}else{progressEl.hidden=true;ledgerBtn.hidden=true;persistentFooter.hidden=true;}saveState();}
export function chapterIntro(ctx,index,title,subtitle,next){ctx.setChapter(index);ctx.html(`<div class="chapter-intro"><div class="chapter-number">${index+1}</div><span class="chapter-theme">${ctx.chapters[index].icon} المرحلة ${index+1} من 9</span><h1 class="display-title">${title}</h1><p class="scene-subtitle">${subtitle}</p><div class="action-row"><button id="chapterNext" class="primary-btn">ادخل المرحلة</button></div></div>`);ctx.$('#chapterNext').addEventListener('click',()=>ctx.go(next));}
