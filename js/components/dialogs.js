export function bindDialogs(ctx){ctx.$('#settingsBtn').addEventListener('click',()=>ctx.settingsDialog.showModal());ctx.ledgerBtn.addEventListener('click',()=>{ctx.renderLedger();ctx.ledgerDialog.showModal();});ctx.$('#brandHome').addEventListener('click',()=>{if(ctx.state.scene==='intro')return;ctx.settingsDialog.showModal();});ctx.$$('[data-close-dialog]').forEach(b=>b.addEventListener('click',()=>ctx.$('#'+b.dataset.closeDialog).close()));ctx.$('#resetProgress').addEventListener('click',()=>ctx.confirmResetDialog.showModal());ctx.$('#confirmReset').addEventListener('click',()=>ctx.resetGame(true));}
