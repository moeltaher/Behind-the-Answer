export function abstraction(ctx,humans,word,line,next){ctx.html(`<div class="abstraction-stage"><div><div class="fade-humans" id="humanGroup">${humans.map(x=>`<span class="human-chip">${x[2]||'●'} ${ctx.h(x[0])}${x[1]?` — ${ctx.h(x[1])}`:''}</span>`).join('')}</div><p class="muted">${ctx.h(line)}</p><div class="abstract-word">${ctx.h(word)}</div><p class="term-note">هذا هو الشكل الذي يظهر به الناتج في المرحلة التالية؛ وقت العمل والضغط والمخاطر لا تظهر معه تلقائيًا.</p><div class="action-row center"><button id="abstractNext" class="primary-btn">تابع إلى المرحلة التالية</button></div></div></div>`);setTimeout(()=>ctx.$('#humanGroup')?.classList.add('faded'),ctx.settings.reduceMotion?20:650);ctx.$('#abstractNext').addEventListener('click',()=>ctx.go(next));}
