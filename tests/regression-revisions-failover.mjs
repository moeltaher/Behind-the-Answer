import { chromium } from 'playwright';
import { load, click, saved } from './helpers/browser-fixtures.mjs';

function baseAdvanced(extra={}){return{dataIndex:1,dataStatuses:['ready'],dataChecks:[{rights:'clear',privacy:'clear',fitness:'clear'}],dataSort:{keep:1,remove:0,redact:0,review:0},dataTrainingUsed:[0],dataCurrentTrainingUsed:[0],candidateRevision:1,trainingIncidentChoice:'pause',trainingRecoveryStage:'verified',evalIndex:3,evalCorrectCount:3,evaluatorCalibrationComplete:true,checkpointEvalComplete:true,safetyChoice:'details',safetyRemediated:true,safetyRetested:true,...extra};}
const browser=await chromium.launch(),page=await browser.newPage({viewport:{width:1280,height:900}});

console.log('REVISIONS_FAILOVER:fast-launch-only-with-real-debt');
await load(page,{scene:'launchDecision',flags:{...baseAdvanced({trainingCheckpoint:'recent',releaseGates:['regression','capacity','risk','rollback'],releaseCapacityStage:'remeasured',extraChecks:['checkpoint']})}});if(await page.locator('#criticalOnly').count())throw new Error('Fast launch remained available after all extra checks completed.');await page.locator('#delayLaunch').waitFor({state:'visible'});

console.log('REVISIONS_FAILOVER:detailed-transfer-feedback');
await load(page,{scene:'transferChallenge',flags:{...baseAdvanced({releaseGates:['regression','capacity','risk','rollback'],releaseCapacityStage:'remeasured',launchChoice:'ready',deployLoad:[45,30,25],deployFailoverChecks:[0,1,2],deployResilienceAccepted:true,deployTrafficOpen:true,deployTabs:['network','compute','model'],deployRecovery:'rollback',deployRecoveryVerifiedFor:'rollback',deployRecoveryDisposition:'cleared',supportIndex:2})}});for(const id of ['weights','retrieval','inference','monitoring','maintenance'])await page.selectOption(`[data-transfer-item="${id}"]`,'build');await click(page,'#transferSubmit');const feedback=await page.locator('#transferFeedback').innerText();if(!feedback.includes('استرجاع مستندات')||!feedback.includes('يحدث مع الطلب الحالي')||!feedback.includes('تشغيل مستمر'))throw new Error('Transfer challenge returns generic feedback.');

console.log('REVISIONS_FAILOVER:historical-lineage-in-results');
await load(page,{scene:'results',flags:{...baseAdvanced({dataIndex:2,dataStatuses:['excluded','ready'],dataChecks:[{rights:'na',privacy:'na',fitness:'na'},{rights:'clear',privacy:'clear',fitness:'clear'}],dataSort:{keep:1,remove:1,redact:0,review:0},dataTrainingUsed:[0,1],dataCurrentTrainingUsed:[1],candidateRevision:2,releaseGates:['regression','capacity','risk','rollback'],releaseCapacityStage:'remeasured',launchChoice:'ready',deployLoad:[45,30,25],deployFailoverChecks:[0,1,2],deployResilienceAccepted:true,deployTrafficOpen:true,deployTabs:['network','compute','model'],deployRecovery:'rollback',deployRecoveryVerifiedFor:'rollback',deployRecoveryDisposition:'cleared',supportIndex:2,transferChoice:'build-use'})}});await page.getByText('استُخدمت تاريخيًا ثم خرجت من النسخة الحالية',{exact:true}).waitFor({state:'visible'});

console.log('REVISIONS_FAILOVER:risk-gate-cannot-lie');
await load(page,{scene:'governanceReview',flags:{...baseAdvanced({dataChecks:[{rights:'unresolved',privacy:'clear',fitness:'clear'}],dataTrainingApproved:[0],releaseGates:[]})}});await page.getByRole('heading',{name:'أغلق حواجز البيانات قبل مراجعة بوابات الإصدار.',exact:true}).waitFor({state:'visible'});if(await page.locator('#toReleaseGates').count())throw new Error('Governance blocker allowed release gates.');

console.log('REVISIONS_FAILOVER:retraining-waits-for-new-setup');
await load(page,{scene:'governanceReview',flags:{...baseAdvanced({dataIndex:2,dataStatuses:['ready','ready'],dataChecks:[{rights:'unresolved',privacy:'clear',fitness:'clear'},{rights:'clear',privacy:'clear',fitness:'clear'}],dataSort:{keep:2,remove:0,redact:0,review:0},dataTrainingApproved:[0],dataTrainingUsed:[0,1],dataCurrentTrainingUsed:[0,1],releaseGates:['regression','rollback']})}});await click(page,'[data-governance-remediate="0"]');let retrained=await saved(page);if(retrained.flags.candidateRevision!==1||retrained.scene!=='trainingSetup')throw new Error('Governance remediation created phantom revision.');if(retrained.flags.releaseGates.length||retrained.flags.checkpointEvalComplete||retrained.flags.safetyRetested||retrained.flags.evalIndex!==0||retrained.flags.governanceEvidenceOpened.length)throw new Error('Revision evidence remained active after retraining plan.');await click(page,'#trainStart');retrained=await saved(page);if(retrained.flags.candidateRevision!==2||retrained.scene!=='trainingRun')throw new Error('Revision 2 was not created at training start.');

console.log('REVISIONS_FAILOVER:distribution-sensitive-failover');
for(const [values,expected] of [[[45,30,25],'1/3 حالات يمكن امتصاصها'],[[60,5,35],'0/3 حالات يمكن امتصاصها']]){await load(page,{scene:'deployFailover',flags:{...baseAdvanced({releaseGates:['regression','capacity','risk','rollback'],releaseCapacityStage:'remeasured',launchChoice:'ready',deployLoad:values})}});for(const index of [0,1,2])await click(page,`[data-failover-check="${index}"]`);await page.getByText(expected,{exact:true}).waitFor({state:'visible'});}

console.log('REVISIONS_FAILOVER:same-scene-focus');
await load(page,{scene:'releaseGateReview',flags:{...baseAdvanced({releaseGates:[],releaseCapacityStage:'idle'})}});await click(page,'[data-gate-investigate="capacity"]');await page.waitForTimeout(50);if(await page.evaluate(()=>document.activeElement?.tagName)!=='H1')throw new Error('Focus was lost after same-scene rerender.');
await browser.close();console.log('Revisions and failover regression checks passed.');
